services:
  pgvector:
    image: pgvector/pgvector:pg16
    environment:
      POSTGRES_DB: scribbleslm
      POSTGRES_USER: scribbleslm
      POSTGRES_PASSWORD: scribbleslm
    volumes:
      - pgvector_data:/var/lib/postgresql/data
    ports:
      - "5433:5432"

volumes:
  pgvector_data:
